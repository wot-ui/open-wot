## 1. 建立文档页可读性样式层级

- [x] 1.1 在 `apps/website/app/globals.css` 的 `.docs-site` 作用域内定义正文、辅助文字、弱化装饰、边框、代码背景和可读强调色等语义变量，避免继续复用单一 `--text-muted` 或散落硬编码灰色。
- [x] 1.2 将正文段落、列表、说明卡片、表格内容、侧边目录、搜索提示和关键交互状态迁移到对应语义变量，确保关键普通文字与实际背景的对比度不低于 4.5:1，并让正文达到更高的阅读对比度。
- [x] 1.3 调整正文、列表、表格描述和搜索输入的中文系统无衬线字体、字号、行高与阅读宽度，同时确认代码、命令、快捷键、品牌和终端标签继续使用 `--font-mono`。

## 2. 消除扫描线阅读干扰

- [x] 2.1 在 `apps/website/app/docs/page.tsx` 或文档页专属样式中移除或彻底禁用覆盖式 `.scan-lines`，确保扫描线不再叠加在标题、正文、导航、表格、代码和交互控件上。
- [x] 2.2 检查文档页背景网格、边框、代码块和强调色，保留不覆盖文字的终端视觉特征，并确认官网首页原有扫描线行为不受影响。

## 3. 保护自定义主题色可读性

- [x] 3.1 在 `apps/website/public/theme.js` 中增加无依赖的颜色对比度计算，并区分用户选择的原始装饰色与用于文字和关键图标的可读强调色变量。
- [x] 3.2 对低亮度自定义主题色生成满足 4.5:1 对比度的派生文字色或稳定后备色，并确保初始加载、颜色输入和恢复默认主题色使用同一套逻辑。
- [x] 3.3 将链接、当前目录项、行内代码和按钮文字继续使用可读强调色，并修复搜索框、主题选择器、重置按钮、复制按钮和文档操作按钮的键盘焦点状态，确保关键焦点边框或 outline 不仅使用低对比度的原始主题色且与实际背景的对比度不低于 3:1。

## 4. 定向验证与兼容性检查

- [x] 4.1 在默认主题色下检查桌面端和移动端 `/docs` 的正文、列表、目录、搜索、表格、代码块和交互状态，确认不存在扫描线覆盖且文字层级清晰。
- [x] 4.2 使用至少一个低亮度自定义主题色验证关键文字的最终对比度不低于 4.5:1，并逐一验证搜索框、主题选择器、重置按钮、复制按钮和文档操作按钮的键盘焦点指示与实际背景的对比度不低于 3:1；同时验证恢复默认主题色后的视觉状态正确。
- [x] 4.3 验证搜索章节、目录跳转、当前章节高亮、代码复制和移动端目录仍保持原有功能行为，且 `/docs` 继续从 `README.md` 生成内容；本变更无需修改 `README.md`。
- [x] 4.4 完成焦点状态修复后重新运行 `pnpm site:lint` 和 `pnpm site:build`，修复本次变更导致的问题并记录验证结果。

name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v1
    with:
      publish: true
      tag: ${{ contains(github.ref_name, '-beta.') && 'beta' || '' }}
    permissions:
      contents: write
      id-token: write
